streamlit
pandas
numpy
plotly
requests
apscheduler
anthropic
scipy
scikit-learn
python-dotenv
aiohttp
httpx
openpyxl
reportlab
yfinance
# ── Tier 3 optional integrations (graceful fallback if not installed) ─────────
# xrpl-py, web3, x402, coinbase-agentkit intentionally excluded from Streamlit
# Cloud requirements — all three are imported with try/except ImportError guards
# in ai_agent.py and data_feeds.py; features degrade gracefully when unavailable.
# To enable XRPL direct reads:  pip install xrpl>=2.4.0
# To enable ERC-4626 web3 reads: pip install web3>=6.0.0
