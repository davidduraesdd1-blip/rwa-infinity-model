streamlit>=1.37.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0
requests>=2.31.0
apscheduler>=3.10.0
anthropic>=0.40.0
scipy>=1.11.0
scikit-learn>=1.4.0
python-dotenv>=1.0.0
aiohttp>=3.9.0
httpx>=0.27.0
openpyxl>=3.1.0
reportlab>=4.0.0
# ── Tier 3 optional integrations (graceful fallback if not installed) ─────────
# xrpl-py, x402, coinbase-agentkit intentionally excluded from Streamlit Cloud
# requirements — all three are imported with try/except ImportError guards in
# ai_agent.py and data_feeds.py; features degrade gracefully when unavailable.
yfinance>=0.2.40
